name: Deploy Angular UI

on:
  push:
    branches:
      - main  # Change if using a different branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18  # Use a stable Node.js version

      - name: Set working directory
        run: cd frontend || exit 1  # 🔹 Adjust this to your actual Angular project folder

      - name: Install dependencies
        run: npm install
        working-directory: frontend  # 🔹 Ensure `package.json` is inside this directory

      - name: Build Angular app
        run: npm run build --if-present
        working-directory: frontend  # 🔹 Ensure build runs inside the correct directory

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: "us-east-1"

      - name: Deploy to S3
        run: aws s3 sync frontend/dist/ s3://ui-thiru --delete
